# 2026-07-23 말풍선 마스크 아이콘 수정

## 문제

- 빠른 설정의 `말풍선` 버튼이 문자 기호 `▰`를 사용해 평행사변형처럼 보인다.
- 실제 워드 클라우드 미리보기는 꼬리가 있는 말풍선인데, 버튼 아이콘이 이를 전달하지 못한다.

## 개선

- `말풍선` 버튼에 둥근 사각형 본문과 아래쪽 꼬리가 보이는 SVG 아이콘을 사용한다.
- 접근성 이름은 기존의 `마스크: 말풍선`을 유지한다.
- 업데이트 내역에 수정 내용을 기록하고, 정적 렌더링 검사에 SVG 아이콘 존재를 추가한다.
